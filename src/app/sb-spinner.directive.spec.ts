import { SbSpinnerDirective } from './sb-spinner.directive';

describe('SbSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new SbSpinnerDirective();
    expect(directive).toBeTruthy();
  });
});
